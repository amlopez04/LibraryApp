<% if object && object.errors.any? %>
  <div class="alert alert-danger">
    <strong><%= pluralize(object.errors.count, "error") %></strong>
    <ul class="mb-0">
      <% object.errors.full_messages.each do |m| %><li><%= m %></li><% end %>
    </ul>
  </div>
<% end %>
